describe('The suite name for Tests', () => {
    it('test1', () => {
        expect(true).to.equal(true)
    }
    )
}
)


// You can use `function` keyword instead =>
describe('The suite name for Tests', function () {
    it('test1', function () {
        expect(true).to.equal(true)
    }
    )
}
)